# Define the function add_two
def add_two(number):
    return number + 2
# Call the function directly and print
print(add_two(4))
# Store the result in a variable and print
result = add_two(8)
print(result)